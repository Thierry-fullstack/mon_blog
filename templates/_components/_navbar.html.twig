<!-- Responsive navbar-->
<nav class="navbar navbar-expand-lg bg-secondary" data-bs-theme="light" id="nav-bar">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_main') }}">Blog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
                {% if app.user %}
                {% if app.user and is_granted('ROLE_ADMIN') %}
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0 text-small">
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_main') }}">Accueil</a></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="dropdown09" data-bs-toggle="dropdown" aria-expanded="false">{{ app.user.pseudo }}</a>
                        <ul class="dropdown-menu text-small" aria-labelledby="dropdown09">
                            <li class="nav-item"><a class="nav-link text-info-emphasis" href="{{ path('app_gestion_profil') }}"><i class="fa-solid fa-industry"></i> Gestion</a></li>
                            <li class="nav-item"><a class="nav-link text-info-emphasis" href="{{ path('app_gestion_show_user',{id:app.user.id}) }}"><i class="fa-regular fa-circle-user"></i> Votre profile</a></li>
                            <li class="nav-item"><a class="nav-link text-info-emphasis" href="{{ path('app_logout') }}"><i class="fa-solid fa-door-open"></i> Sortie</a></li>
                        </ul>
                    </li>
                </ul>
                {% elseif app.user and is_granted('ROLE_AUTHOR') %}
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0 small">
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_main') }}">Accueil</a></li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="dropdown09" data-bs-toggle="dropdown" aria-expanded="false">{{ app.user.pseudo }}</a>
                            <ul class="dropdown-menu small" aria-labelledby="dropdown09">
                                <li class="nav-item"><a class="nav-link text-info-emphasis" href="{{ path('app_gestion_profil') }}"><i class="fa-solid fa-industry"></i> Gestion</a></li>
                                <li class="nav-item"><a class="nav-link text-info-emphasis" href="#"><i class="fa-regular fa-circle-user"></i> Profile</a></li>
                                <li class="nav-item"><a class="nav-link text-info-emphasis" href="{{ path('app_logout') }}"><i class="fa-solid fa-door-open"></i> Sortie</a></li>
                            </ul>
                        </li>
                    </ul>
                {% elseif app.user and is_granted('ROLE_USER') %}
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0 small">
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_main') }}">Accueil</a></li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="dropdown09" data-bs-toggle="dropdown" aria-expanded="false">{{ app.user.pseudo }}</a>
                            <ul class="dropdown-menu" aria-labelledby="dropdown09">
                                <li class="nav-item"><a class="nav-link" href="#"><i class="fa-regular fa-circle-user"></i>Profile</a></li>
                                <li class="nav-item"><a class="nav-link text-info-emphasis" href="{{ path('app_logout') }}"><i class="fa-solid fa-door-open"></i> Sortie</a></li>
                            </ul>
                        </li>
                    </ul>
                {% endif %}
                {% else %}
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0 small">
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_main') }}">Accueil</a></li>
                        <li class="nav-item"><a class="nav-link" href="#!">A propos</a></li>
                        <li class="nav-item"><a class="nav-link" href="#!">Contact</a></li>
                        <li class="nav-item"><a class="nav-link" aria-current="page" href="{{ path('app_register') }}">S'inscrire</a></li>
                        <li class="nav-item"><a class="nav-link" aria-current="page" href="{{ path('app_login') }}">Se connecter</a></li>
                    </ul>
                {% endif %}
        </div>

    </div>
</nav>
