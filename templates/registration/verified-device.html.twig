{% extends 'base.html.twig' %}

{% block title %}Verified device{% endblock %}

{% block body %}
    <h1>Verified device</h1>
    {% if exception is defined and exception is not null %}
        {{ exception }}
    {%  endif %}


    {{ form(form) }}

{% endblock %}
