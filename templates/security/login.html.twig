{% extends 'base.html.twig' %}

{% block title %}Connection{% endblock %}
{% block importmap %}
    {{ importmap(['app', 'login']) }}
{% endblock %}

{% block body %}
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    <div class="container mt-5 ">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8 col-sm-12">
                <div class="" id="bg-login-form">
                    <div class="card-body">
                        <div>
                            <h1 class="card-title text-center fst-italic text-capitalize text-warning fw-semibold">Connexion</h1>
                        </div>
                        <hr class="text-primary-emphasis" />
                        <div class="d-grid gap-2 mb-3">
                            <ul class="list-group">
                                <li id="dialogLogin" class="list-group-item text-primary-emphasis fw-bolder"></li>
                            </ul>
                        </div>
                        <form method="post" id="loginForm" novalidate>
                            <div class="form-floating mb-3">
                                <input type="email" value="{{ last_username }}" class="form-control text-danger-emphasis " name="email" id="inputEmail" required
                                       autocomplete="email" >
                                <label for="inputEmail" class="form-check-label text-primary-emphasis" id="labelEmail">Email *</label>
                            </div>
                            <div class="form-floating mb-3">
                                <input type="password" name="password" id="inputPassword" class="form-control  text-danger-emphasis"  required
                                       autocomplete="current-password" >
                                <label for="inputPassword" class="form-check-label text-primary-emphasis" id="labelPassword">Mot de passe *</label>
                            </div>
                            <div class="mb-3 form-check">
                                <input type="checkbox" name="_remember_me" class="form-check-input text-danger-emphasis" id="inputRemember">
                                <label class="form-check-label text-primary-emphasis" for="inputRemember" id="labelRemember"><small class="mx-2">Se souvenir de moi</small></label>
                            </div>
                            <div class="d-grid">
                                <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">
                                <button type="submit" class="btn btn-outline-light text-primary-emphasis text-capitalize" id="submitConnect">Soumettre</button>
                            </div>
                        </form>
                        <div class="text-center mt-3">
                            <a href="{{ path('app_forgot_password_request') }}" class="text-warning fw-semibold fst-italic text-decoration-none "
                               data-bs-toggle="tooltip" data-bs-placement="top" title="Mot de passe oublié">Mot de passe oublié ?</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
